// 2. Add and change
// Add 'toy' to the cat object.
// Change the 'age' to another number.

cat = {
    name:"frajola",
    color: "branco",
    age: 3,
};

cat.toy = "pelúcia";
cat.age = 4; 

console.log(`Nome: ${cat.name}\nCor: ${cat.color}\nIdade: ${cat.age}\nBrinquedo: ${cat.toy}`);